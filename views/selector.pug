extends layout

block content
  h1= title
  - var image_names= ["BlackdevilMask.jpg","Blackmask.jpg","Read and white mask.jpg","RedDevilmask.jpg","Whitedevilmask.jpg"]
  - for(let i=0;i<3;i++){
     - let randValue= Math.floor(Math.random()*5)
     img(src=`/images/${image_names[randValue]}` alt=`Image No ${randValue}`)

  - }